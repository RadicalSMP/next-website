# 常用命令
- `bun install` 安装依赖
- `bun run dev` 启动开发环境
- `bun run build` 生产构建验证
- `bun run lint .` 运行 ESLint
- `git status --short --branch` 查看工作区状态
- Windows Shell 常用：`Get-Content <path>` 读文件，`Get-ChildItem` 列目录，`rg -n "pattern"` 搜索文本